* text=auto eol=lf

# Remove commit-msg hook from repo statistics.
scripts/commit-msg.sh -linguist-detectable
# Remove flake.nix from repo statistics.
flake.nix -linguist-detectable
# Remove .envrc from repo statistics.
.envrc -linguist-detectable
# Remove gitlab.rb from repo statistics.
gitlab.rb -linguist-detectable
